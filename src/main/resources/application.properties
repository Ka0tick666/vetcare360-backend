spring.application.name=vetcare360-backend
server.port=${PORT:8080}

# URL del emisor de Cognito (Issuer URI)
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://cognito-idp.us-east-1.amazonaws.com/us-east-1_Gz8tIv6dd
logging.level.org.springframework.security=DEBUG
# Supabase PostgreSQL Setup
spring.datasource.url=jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=${DB_USERNAME:postgres.ptrkxumyvnwecrnhfigr}
spring.datasource.password=${DB_PASSWORD:3D%r/TDNS5#uUqa}

# Spring Data JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect